(()=>{const U='https://qgrsbqzhbifbyyfbslyh.supabase.co',K='sb_publishable_DKxNrYb1zbLkKyQQ_LAh2w_B0NRK72k',SK='jgos:session';let refreshing=null;function get(){try{return JSON.parse(localStorage.getItem(SK)||'null')}catch{return null}}function expiry(s){return s?.expires_at||((s?.expires_in&&s?.created_at)?Number(s.created_at)+Number(s.expires_in):null)}function nearExpiry(s){const x=expiry(s);return x?Date.now()/1000>=Number(x)-90:false}function clear(msg){localStorage.removeItem(SK);sessionStorage.setItem('jgos:sessionMessage',msg);location.reload()}function showMessage(){const m=sessionStorage.getItem('jgos:sessionMessage');if(!m)return;sessionStorage.removeItem('jgos:sessionMessage');setTimeout(()=>{const el=document.getElementById('loginMsg');if(el)el.textContent=m},250)}async function refresh(s){if(refreshing)return refreshing;if(!s?.refresh_token)return clear('Ihre Sitzung ist abgelaufen. Bitte melden Sie sich erneut an – Ihre gespeicherten Daten bleiben erhalten.');refreshing=(async()=>{try{const r=await fetch(U+'/auth/v1/token?grant_type=refresh_token',{method:'POST',headers:{apikey:K,'Content-Type':'application/json'},body:JSON.stringify({refresh_token:s.refresh_token})});if(!r.ok)throw Error('refresh failed');const n=await r.json();n.created_at=Math.floor(Date.now()/1000);localStorage.setItem(SK,JSON.stringify(n));window.dispatchEvent(new CustomEvent('jgos:session-refreshed',{detail:n}));return n}catch{clear('Ihre Sitzung konnte nicht erneuert werden. Bitte melden Sie sich erneut an.')}finally{refreshing=null}})();return refreshing}async function check(){const s=get();if(s&&nearExpiry(s))await refresh(s)}window.addEventListener('unhandledrejection',e=>{const m=String(e.reason?.message||e.reason||'').toLowerCase();if(m.includes('jwt expired')||m.includes('invalid jwt')||m.includes('refresh token')){const s=get();if(s?.refresh_token)refresh(s);else clear('Ihre Sitzung konnte nicht erneuert werden. Bitte melden Sie sich erneut an.')}});document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible')check()});window.addEventListener('pageshow',check);document.readyState==='loading'?document.addEventListener('DOMContentLoaded',()=>{showMessage();check()}):(()=>{showMessage();check()})()})();