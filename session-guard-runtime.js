(()=>{const SK='jgos:session';function get(){try{return JSON.parse(localStorage.getItem(SK)||'null')}catch{return null}}function expired(s){const x=s?.expires_at||s?.expires_in&&s?.created_at?Number(s.created_at)+Number(s.expires_in):null;return x?Date.now()/1000>=Number(x)-20:false}function clear(msg){localStorage.removeItem(SK);sessionStorage.setItem('jgos:sessionMessage',msg);location.reload()}function showMessage(){const m=sessionStorage.getItem('jgos:sessionMessage');if(!m)return;sessionStorage.removeItem('jgos:sessionMessage');setTimeout(()=>{const el=document.getElementById('loginMsg');if(el)el.textContent=m},250)}function check(){const s=get();if(s&&expired(s))clear('Ihre Sitzung ist abgelaufen. Bitte melden Sie sich erneut an – Ihre gespeicherten Daten bleiben erhalten.')}window.addEventListener('unhandledrejection',e=>{const m=String(e.reason?.message||e.reason||'').toLowerCase();if(m.includes('jwt expired')||m.includes('invalid jwt')||m.includes('refresh token'))clear('Ihre Sitzung konnte nicht erneuert werden. Bitte melden Sie sich erneut an.')});document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible')check()});window.addEventListener('pageshow',check);document.readyState==='loading'?document.addEventListener('DOMContentLoaded',()=>{showMessage();check()}):(()=>{showMessage();check()})()})();